HERMES_COMPANION_HOST=127.0.0.1
HERMES_COMPANION_PORT=17787

# Optional allow-list for browser origins that may call the local loopback.
# Comma-separated. By default the server allows loopback WebUI origins.
# HERMES_COMPANION_ALLOWED_ORIGINS=http://127.0.0.1:8787,http://localhost:8787

